<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title - Admin System</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">

    <style>
        body {
            display: flex;
            min-height: 100vh;
            overflow-x: hidden;
        }

        #sidebar {
            min-width: 250px;
            max-width: 250px;
            background: #343a40;
            color: #fff;
            transition: all 0.3s;
        }

            #sidebar .nav-link {
                color: rgba(255,255,255,.8);
                padding: 15px 20px;
                border-bottom: 1px solid rgba(255,255,255,.1);
            }

                #sidebar .nav-link:hover, #sidebar .nav-link.active {
                    color: #fff;
                    background: #495057;
                }

        #content {
            width: 100%;
            padding: 20px;
            background-color: #f8f9fa;
        }
    </style>
</head>
<body>
    <nav id="sidebar">
        <div class="p-4 text-center">
            <h4 class="fw-bold">ADMIN PAGE</h4>
        </div>
        <ul class="list-unstyled components">
            <li>
                <a href="@Url.Action("Index", "HomeAdmin", new { area = "Admin" })" class="nav-link ">
                    <i class="bi bi-grid-3x3 me-2"></i>  Dashboard
                </a>
            </li>
            <li>
                <a href="@Url.Action("Index", "Product", new { area = "Admin" })" class="nav-link ">
                    <i class="bi bi-box-seam me-2"></i> Quản lý Sản phẩm
                </a>
            </li>
            <li>

                <a href="@Url.Action("Index", "Order", new { area = "Admin" })" class="nav-link text-white">
                    <i class="bi bi-receipt me-2"></i> Quản lý Đơn hàng
                </a>
            </li>
            <li>

                <a href="@Url.Action("Index", "DoanhThu", new { area = "Admin" })" class="nav-link text-white">
                    <i class="bi bi-cash me-2"></i> Quản lý Doanh Thu
                </a>
            </li>
            <li>
                <a href="@Url.Action("Index", "Home", new { area = "" })" class="nav-link text-warning">
                    <i class="bi bi-house-door me-2"></i> Về trang chủ
                </a>
            </li>

            <li class="mt-3">
                <a href="@Url.Action("Logout", "HomeAdmin", new { area = "Admin" })" class="nav-link bg-danger text-white">
                    <i class="bi bi-box-arrow-right me-2"></i> Đăng xuất
                </a>
            </li>
        </ul>
    </nav>

    <div id="content">
        <nav class="navbar navbar-light bg-white shadow-sm mb-4 rounded">
            <div class="container-fluid">
                <span class="navbar-brand mb-0 h1"> </span>
            </div>
        </nav>

        @RenderBody()
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    @RenderSection("scripts", required: false)
</body>
</html>